{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "apt-get update && sudo apt-get install -y binutils libproj-dev gdal-bin && sudo echo /usr/local/lib >> /etc/ld.so.conf && sudo ldconfig && echo $(cat /etc/ld.so.conf) && python manage.py migrate && gunicorn portfolio_core.wsgi",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
    }
}